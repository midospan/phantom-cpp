#include "phantom/phantom.h"
#include "composition.h"
#include "composition.hxx"

o_registerNT((phantom), (typename), (t_Component), composition);
o_registerNT((phantom), (typename), (t_Component), composition_replacer);
o_registerNT((phantom), (typename), (t_Component), composition_inserter);