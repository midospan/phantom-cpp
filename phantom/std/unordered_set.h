#ifndef o_std_unordered_set_h__
#define o_std_unordered_set_h__

#include <unordered_set>

#include "set.h"

/// UNORDERED MAP
// unordered_set
o_declareNT(class, (std), (typename, typename, typename, typename), (t_Kty, t_Hash, t_Keyeq, t_Alloc), unordered_set);

#endif // o_std_unordered_set_h__