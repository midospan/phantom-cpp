#ifndef o_std_unordered_map_h__
#define o_std_unordered_map_h__

#include <unordered_map>

#include "map.h"

/// UNORDERED MAP
o_declareNT(class, (std), (typename, typename, typename, typename, typename), (t_Kty, t_Ty, t_Hash, t_Pr, t_Alloc), unordered_map);

o_declareNT(class, (phantom), (typename, typename, typename, typename, typename), (t_Kty, t_Ty, t_Hash, t_Pr, t_Alloc), unordered_map);

#endif // o_std_unordered_map_h__