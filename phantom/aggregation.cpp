#include "phantom/phantom.h"
#include "aggregation.h"
#include "aggregation.hxx"

o_registerNT((phantom), (typename), (t_Aggregate), aggregation);
o_registerNT((phantom), (typename), (t_Aggregate), aggregation_replacer);
o_registerNT((phantom), (typename), (t_Aggregate), aggregation_inserter);