#ifndef o_phantom_unordered_map_h__
#define o_phantom_unordered_map_h__

#include <std/unordered_map.h>

#include "map.h"

/// UNORDERED MAP
o_declareNT(class, (phantom), (typename, typename, typename, typename), (t_Kty, t_Ty, t_Hash, t_Pr), unordered_map);

#endif // o_phantom_unordered_map_h__