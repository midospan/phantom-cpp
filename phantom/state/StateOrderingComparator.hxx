#pragma once
#include "phantom/util/Comparator.hxx"
o_classNS((phantom, state), StateOrderingComparator, (Comparator))
{
    o_reflection {};
};
o_exposeN((phantom, state), StateOrderingComparator);

